import { DateRangePicker } from './DateRangePicker';
import { DateTime, Duration } from 'luxon';

// Exporta la clase principal y también los tipos Luxon para que el usuario pueda
// pasar objetos DateTime y Duration fácilmente.
export { DateRangePicker, DateTime, Duration };